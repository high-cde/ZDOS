# Device policy for BusyBox mdev; unspecified nodes retain safe defaults.
null      root:root 0666
zero      root:root 0666
random    root:root 0666
urandom   root:root 0666
console   root:root 0600
tty[0-9]* root:root 0620
ttyS[0-9]* root:root 0620
